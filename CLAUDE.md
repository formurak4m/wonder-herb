# CLAUDE.md — Wonder Herb Admin Rebuild

Context for Claude Code. Read this before making changes. Keep it updated as the project moves.
Reflects the actual repo as of 16 September 2026 (branch `rebuild/editor`). Where this file and the code disagree, the code wins. Fix this file.

## What this project is

Turn the Wonder Herb (康草堂) website into a site the client edits themselves through a Wix-like admin,
while the public site stays fast static HTML on GitHub Pages.

- The client is currently on Wix. We are offering a switch. The admin must feel very similar to Wix.
- We are NOT cloning Wix. We rebuild only the parts the client actually uses.
- Deadline: roughly one month for a usable slice, a second month for the full switch.

## Current state (read first — a lot already works)

**Stack:**
- **API:** Node + Express + MongoDB.
- **Public pages:** 18 pages: 4 hand-coded static HTML files plus 14 pre-rendered (every content page and all six product detail pages — one tree each in `data/pages/`; their hand-coded originals are kept in `legacy/`). The four left: `index.html` (Phase 9's last page) and `product.html` / `購物車.html` / `account.html`, which are out of Phase 9 scope by decision.
- **React 19** runs only on the server, in the renderer (`renderToStaticMarkup`), and in the Puck editor app (Vite). **No React ships to the public site.**
- **Sections** are bundled for Node with esbuild (`npm run sections:build`).

**Build progress** (from git history; the checklist boxes in `BUILD_TASKS.md` have not been ticked):
- **Done:** Phases 0–4, Phase 6, Phase 7, P5-T1, P8-T1, P8-T2.
- **Not done:**
  - **P5-T2**: `buildSite`, so the renderer writes only to gitignored `renderer/.out/` and nothing pre-rendered is deployed yet.
  - **P8-T3**: the deploy allow-list, registered but not applied. The deploy is still the whole repo.
- **Phase 9: 14 of 18 pages DONE (16 Sep 2026); every content page and every product detail page is migrated.** Their originals are retired to `legacy/`, which carries noindex and is disallowed in robots.txt. Every visual diff against the 15 Sep baseline is reconciled to the pixel. Left in Phase 9: `index.html` only. **product.html, 購物車.html and account.html are OUT of Phase 9 scope** (owner, 16 Sep 2026) and stay hand-coded — see P9-T1 in BUILD_TASKS for the reasons. Per-page cost, what each one found and the re-estimate are under P9-T1 in `BUILD_TASKS.md`. Pages whose original is in `legacy/` are written to the root by `npm run render`; never hand-edit them.

Work happens on `rebuild/editor` (off `geo`), pushed to origin. `main` is untouched, and **nothing from this branch is live**. Never push to or touch `main`. The owner commits.
Hosting is deliberately deferred to Phase 11: everything runs locally until then. Do not connect or authorise the Vercel connector before Phase 11.

Already built:
- **Express + MongoDB API** in `server/`:
  - auth, with a protected super-admin (rotated 15 Sep 2026, finding 22) and a per-module permission model;
  - products, stock, sales, invoices, reports;
  - page trees (`/api/pages/:slug`) and draft preview (`POST /api/preview`, through the real renderer).
- **Hybrid publish model**: MongoDB is the editing surface.
  - `npm run publish` runs export (DB → `data/`), `sections:build`, render, the SEO gate (`test:seo`) and the published-data leak gate (`test:data`), and stops on any failure.
  - The owner commits. Deploy is GitHub Actions (`.github/workflows/static.yml`) → GitHub Pages on push to `main`, custom domain via `CNAME`.
  - If the API is off, admin and site fall back to the committed files.
- **Content as data**: `data/products.json` (per-language titles/descriptions, finding 19), `data/cases.json`, `data/faq.json`, `data/homepage.json`, `data/inventory.csv`, and page trees in `data/pages/<slug>.json` (8: products, faq, contact, brochure, testing, articles, research, cases).
- **Renderer** (`renderer/`): page tree + `data/` → a complete static HTML document with content, JSON-LD, hreflang, canonical and meta.
  - Site chrome is lifted from a live page (`assets.chromeFrom`, a stopgap).
  - Page CSS comes from `assets/page-<slug>.css` plus `assets/chrome.css` linked last. These are generated by `scripts/extract-css.js`; don't hand-edit them.
- **Sections** (`sections/`): 14 components shared by the editor and the renderer (page-header, hero, text-block, product-grid, product-detail, gallery, related-products, contact-cards, cta-band, faq-accordion, callout, card-grid, map-embed, case-list). `card-grid` carries 7 variants and `text-block` 4: a variant is always markup that exists on the live site, never an invention.
- **Behaviour for pre-rendered pages** (`assets/site.js` shared, plus `assets/behaviour/quick-view.js`):
  - `site.js`: cart, badge, phone menu, language switcher; per-section files add quick view (product-grid), the case filter (case-list), the buy panel (product-detail) and the gallery thumbnails;
  - loaded by `renderer/template.js` according to the tree's section types.
- **Puck editor** (`editor/`, `npm run editor:dev`): edits page trees one language at a time and saves through the API. Local only and **not client-facing yet** (P11-T2 makes it one admin site with one login).
- **Pages CMS pilot** (`.pages.yml`): git-based form editor, wired for Cases only. Uses the 7-language object model.
- **Admin** is a single vanilla file: `admin/index.html`.
- **7 languages**: `zh` (primary) + `en, de, es, fr, ja, ru` (optional, fall back to `zh`). Every text field is a per-language object. Month 1 renders `zh` only (`LANGS_IN_SCOPE`).
- **Visual + SEO baseline captured** (Phase 0, re-captured 15 Sep 2026 at P9-T1): `node scripts/baseline.js`, output in `baseline/` (gitignored).
  - **What it records:** all 18 pages at 1280px and 390px, **scripts on** (`baseline/screens/`) **and scripts off** (`baseline/screens-nojs/`), plus each page's rendered `<head>` (`baseline/head/`).
  - **How:** a static server with the API off, under a **production-like hostname** (`wonder-herb.test`, mapped inside Chromium), so it reflects what GitHub Pages serves.
  - **Why the hostname matters:** the live pages change by hostname. On `localhost` they show an account icon that makes the 1280 nav 190px instead of 164px. The first baseline was taken on localhost and was off by ~15% of pixels at 1280 for that reason alone, so every visual diff before the re-capture was measured against a wrong reference (`docs/FINDINGS.md` finding 21).
  - Set `BASELINE_SITE` to a checkout of HEAD so the reference records committed content. The old localhost set is archived in `baseline/_previous-localhost-2026-09-08/`.
  - **Known remaining gap:** scripts-on captures use the CJK fallback font for `lang="zh"`, because the live language script overwrites `zh-Hant` (finding 21b, font stack undecided).
  - Re-run it if the live pages change before migration.
- **Test suite**: `npm run test:all` = 15 suites, 1,439 passing checks, green (16 Sep 2026).
  - Suites: i18n, sections, render, seo, published-data, language assets, behaviour, editor config, editor language, API, auth, sales, UI + Mongo, UI auth, UI sweep.
  - `jsdom`, `playwright`, `vite`, `esbuild` and `@puckeditor/core` are devDependencies. Nothing new ships to the public site.
  - The Mongo-backed suites use `wonderherb_test`. **Never run two suites against one Mongo at once**: it fakes auth regressions.

The two real gaps (both partly closed, neither closed for a visitor yet):
1. **Content is rendered client-side on 4 of the 18 pages.** Those are static shells that `fetch('./data/*.json')` and build content in the browser, so crawlers mostly see an empty shell. The 14 migrated pages are pre-rendered (content in the HTML) — including 典型病例, whose 15 case studies used to exist only after JavaScript ran, and 產品_憶活素, whose medical citations were injected into an empty box. Nothing from this branch is deployed until it merges to `main`.
2. **The client cannot yet add or reorder sections.** The Puck editor and section model exist locally, but 4 of 18 page layouts are still hand-coded, and the editor isn't reachable by the client until Phase 11.

Other issues:
- **Heavy media in the repo:** ~75 MB of `.glb` 3D models (6 files) are committed and redeploy on every push. Move to R2/CDN (Phase 10).
- **Media is not only the `.glb` files.** The homepage background video is hosted on the client's Wix CDN, and the product photos are hotlinked from Google Drive. Both must move to R2 in Phase 10; the Wix video dies at cutover.
- **Hosting is split:** static site on GitHub Pages, admin/API config points at Vercel (`vercel.json`). The API and MongoDB are not hosted (`MONGO_URL` is localhost).
- **Which site is actually live** (checked 15 Sep 2026):
  - `www.wonder-herb.com` and the apex domain resolve to **Wix**. That is the client's real site until cutover (Phase 18), despite the `CNAME` file in this repo.
  - The GitHub Pages deploy of `main` is publicly reachable at `formurak4m.github.io/wonder-herb/`, and anything on `main` is served there.
- **The repo is PUBLIC.**
  - `*.xlsx` is gitignored: the root spreadsheets hold real customer names, phones and medical remarks.
  - `customers` and `invoices` are never exported to `data/` or exposed via `/api/cms`.
  - Credentials live only in the gitignored `.env`.
  - Old commits on `rebuild/editor` still contain former staff login addresses and an invented product claim. The owner decided against rewriting history (finding 22).
  - **Finding 26 (CRITICAL, potentially a legal and privacy matter under HK PDPO; needs legal advice, the owner handles it with the client):** real case-study patients' identities were attached to fabricated five-star product reviews with invented ratings, publicly served on this repo's GitHub Pages copy from 4 Jun to 15 Sep 2026 (the client's Wix site was unaffected). Removed and verified (`6503a01` on `main`; `7a918b6`, `d2ee9c4` here); `test:seo` fails any rating data. **The published content is evidence, preserved in git history** (`1201c70` on `main`): **never rewrite `main`'s history, never force-push, never delete `geo` or `rebuild/editor`** while this is open. **Do not investigate further:** no fetching the client's Wix site, no tracing authorship through commit metadata. Background only since 15 Sep 2026: site content is not authoritative, and claims provenance is not a migration gate.
- **Site content is NOT authoritative (owner, 15 Sep 2026):** the client will replace all of it. Content disputes are not blockers, and nothing further is to be investigated, escalated or changed on them unless the owner asks. **Don't change site data unless the owner explicitly asks.** Prices come from `data/products.json` (no price hold); PSP-500's out-of-stock state is committed.
- Currency is HKD.

Known issues are logged in `docs/FINDINGS.md` (31 findings so far), each with the phase that owns its fix. The index table at the top shows which are fixed. **Do not fix a finding out of its phase** unless the owner says so.

## The two changes we are adding (in order of value)

### Change 1 — Pre-render the pages (highest value, protects SEO/GEO)
The publish step renders content **into** the HTML instead of leaving the browser to fetch it. Same data, same `export -> commit -> deploy` pipeline. Output includes content, JSON-LD, hreflang, canonical and meta, so crawlers see the real page. Fixes gap 1 and is the most important thing in the project. The renderer and SEO gate are built; deploying rendered pages is P5-T2 plus Phase 9.

### Change 2 — A Wix-like visual editor (Puck)
**Puck** (`@puckeditor/core`, MIT, self-hosted, React) for visual page building: click the real page, edit, add / reorder / duplicate / delete sections, publish. It runs as its own editor app (`editor/`) that talks to the existing Express API. Puck's permissions API locks destructive actions (Phase 12). Built locally at P8; client access is P11-T2.

## Non-negotiables (do not violate)

1. **Public site stays static HTML on GitHub Pages.** A broken admin must never take the live site down. Preserve the existing fallback-to-committed-files behaviour. (This protects the public site; admin and editor are tools, see P11-T2.)
2. **SEO must survive and improve.** Every page passes an SEO assertion check (JSON-LD valid, hreflang reciprocal, canonical correct, meta present, headings preserved) before publish. Do NOT chase byte-identical HTML; gate on the SEO/semantic check plus a visual diff within tolerance.
3. **Two editors, clear boundary.** Pages CMS / existing admin for content lists (products, cases, FAQ, homepage). Puck for page layouts. Do not rebuild in Puck what forms + API already do.
4. **Editor instrumentation must not ship to production.** `data-wh-*` selection attributes are editor/preview only. Published output is clean. Published behaviour hooks use plain `data-sku` / `data-price` / `data-status`.
5. **Sections, not free-form dragging.** Add / move / duplicate / delete + 2 to 3 layout variants per section. No absolute pixel positioning.
6. **Every text field is a per-language object.** 7 languages, fall back to `zh`. This model already exists in `.pages.yml` and `data/`; keep it.
7. **Heavy assets leave the repo.** New media and `.glb` models go to R2/CDN, not Git.

## Repo layout (actual)

```
/                         public static site + config
  index.html              homepage (client-side i18n)
  product.html            generic product page, fetches ./data/products.json
  account.html
  產品介紹.html etc.        the other CJK-named pages (18 pages total, ~49k lines)
  *.glb                   3D bottle models (~75MB, in Git - Phase 10 moves them)
  *.xlsx                  real sales/inventory spreadsheets - GITIGNORED, customer PII
  sitemap.xml robots.txt llms.txt CNAME
  data/                   products.json, cases.json, faq.json, homepage.json, inventory.csv
    pages/                page trees, one per migrated page (14 so far)
  assets/                 published CSS + behaviour for pre-rendered pages
    chrome.css page-<slug>.css      GENERATED by scripts/extract-css.js
    site.js               shared behaviour: cart, badge, phone menu, language switcher
    behaviour/            per-section behaviour (quick-view, case-list, product-detail, gallery)
  sections/               14 section components, shared by Puck AND the renderer
  renderer/               render.js (tree -> HTML), head.js (SEO head), template.js
                          (document shell, reveal + behaviour guards), i18n.js, build-sections.js
    sample/               P5-T1 fixture tree, used only by test:render
    .build/ .out/         gitignored: sections bundle, rendered output
  editor/                 Puck React app (Vite), talks to the API
  admin/index.html        vanilla admin (single file, no framework)
  api/cms.js              small helper
  server/                 Express API
    index.js              main API (~36 KB); MongoDB is the editing surface
    auth.js db.js sales.js sales-report.js stock.js reports.js
  scripts/                export, publish, seed, create-admin, import-sales, baseline,
                          extract-css, migrate-products (one-time), rotate-super-admin, test-*.js
  docs/FINDINGS.md        every known issue, its severity and owning phase - read the index
  BUILD_TASKS.md          the phased plan: how each task is done and verified
  SETUP.md                local database setup;  ADMIN_GUIDE.md  client-facing editing guide
  PROJECT_STATUS.md       older handoff note from 13 Aug 2026 (pre-rebuild) - superseded by this file
  baseline/               gitignored visual + head references (see above)
  .github/workflows/static.yml   deploy repo to GitHub Pages on push to main (P8-T3 will allow-list it)
  .pages.yml              Pages CMS config (Cases pilot)
  vercel.json             /admin rewrite + CORS headers for /api and /*.glb
  package.json            deps: cors, exceljs, express, mongodb, react, react-dom
                          devDeps: @puckeditor/core, vite, @vitejs/plugin-react, esbuild, jsdom, playwright
  .env / .env.example     MONGO_URL, MONGO_DB=wonderherb, PORT=4000, SUPER_ADMIN_*  (.env.* ignored)
```

## Commands (actual, from package.json)

```
npm run dev              # Express API + admin at :4000
npm run seed             # seed MongoDB from data/
npm run export           # DB -> data/*.json + data/pages/*.json (writeIfChanged: unchanged runs leave git clean)
npm run publish          # export -> sections:build -> render -> test:seo -> test:data; stops on failure
npm run sections:build   # bundle sections/ for the Node renderer
npm run render           # page trees -> renderer/.out/ (gitignored; P5-T2 writes to the site)
npm run editor:dev       # Puck editor (Vite);  editor:build for a production bundle
npm run admin:create     # create an admin account
npm run import:sales     # import the sales spreadsheet
npm run test:all         # all 15 suites
npm run test:seo         # SEO gate on every tree in data/pages/
npm run test:data        # no private data/emails in data/ or any tracked file
npm run test:lang-assets # a migrated page carries the zh asset, never another language's (finding 28)
npm run test:behaviour   # migrated pages' cart/menu/quick view/language, by effect, with negative controls (every tree in data/pages/)
npm run test:render | test:sections | test:i18n | test:editor | test:editor:lang
npm run test:api | test:auth | test:sales | test:ui | test:ui:auth | test:sweep
node scripts/baseline.js # re-capture the visual + head baseline (see above)
# deploy: commit to main -> GitHub Actions (.github/workflows/static.yml) -> GitHub Pages
```

## Month 1 scope (usable slice, Wix stays live)

Sequencing follows `BUILD_TASKS.md`: renderer, editor and page migration come first, and hosting is Phase 11.

1. Host the API + MongoDB for real (Vercel + MongoDB Atlas free tier, or a small VPS). Confirm the fallback-to-files behaviour still holds.
2. Move media + the `.glb` models to R2/CDN; update references and `vercel.json`/page markup.
3. **Renderer: pre-render the core pages** (home, products, product-detail, contact) with the SEO assertion check passing.
4. **Puck**: stand up the editor app, build 8 to 10 core section components, wire permissions.
5. Migrate the core 4 to 5 page layouts to data, Chinese first (other 6 languages fall back to `zh`).
6. Keep Pages CMS as the content-editing path so the client has something usable from week one.
7. Demo. Wix stays live as backup.

Month 2: all 18 pages, all 7 languages, full ~22 sections, add-page-from-template, forms (visitor submissions), dashboard parity (contacts / orders / analytics), security hardening, staging, then cut over and cancel Wix.

## Conventions

- Section components are pure, take a per-language `fields` object, and render identically in Puck and in the renderer (one source of truth avoids Puck's canvas-vs-site fidelity gap).
- Keep `zh` as the fallback everywhere, matching `.pages.yml`.
- Prefer plain-text fields with a constrained formatter over raw `contenteditable`; watch Chinese IME across all 7 languages.
- Any new section type needs:
  - the component;
  - its Puck field config;
  - 1 to 3 layout variants;
  - a renderer path that emits SEO-safe HTML;
  - a fidelity map against the live markup;
  - its behaviour file, if it is interactive (registered in `renderer/template.js` `BEHAVIOURS`).
- **Identify products by SKU, never by `id`.** Database ids, the old pages' cart ids and the live catalogue's ids all differ (findings 19, 23).
- **The store merges, never replaces:** no write path may delete a field its caller was not shown (finding 25, being fixed at P12-T3).
- **Verify by effect, with a negative control.** A check must fail when the thing it tests is removed. "No errors" is not verification (findings 12, 23).
- Preserve the existing test culture: every migrated page runs the SEO gate and `test:behaviour`.

**Working rules the owner has set:**
- Work one task at a time.
- State intent and files before starting.
- Run each Verify and show real output.
- Stop at phase gates.
- Never install anything without saying so first.
- **Git:** never push or touch `main`; the owner commits. Never use a working-tree-discarding command (`git checkout --`, `git restore`, `git reset --hard`) to find something out.
- **Processes and data:**
  - Check what is running before starting or killing anything that binds a port or touches MongoDB, and kill only PIDs you started.
  - Never write a value you know to be false into the database to tidy a diff (finding 17).
- **Sensitive material:**
  - Never quote leaked material (emails, credentials, customer data) verbatim in docs, tests, commits or chat. Describe it, cite its location, redact the values (finding 22).
  - Test fixtures carry no real customer data, no product health claims and no invented ratings.

## Decisions already taken (do not relitigate)

- **D1:** sticky nav plus `overflow-x: clip` instead of a JS nav offset.
- **D2:** one stylesheet per page, with `chrome.css` linked last (finding 15).
- **A tree reproduces the live `<section>` wrappers** (`wrap: { section, label, container }`): those classes carry padding, and adjacent nodes naming the same section share one wrapper. Dropping them moves the whole page (P9 batch).
- **Per-language media is NOT carried** (finding 28): 小册子 swaps its brochure scan per language, and a tree holds one URL. Check every page for this before migrating it — only the visual diff catches it.
- **Identity rotation, not a history rewrite**, for the exposed logins (finding 22).
- **Migrated pages are Chinese-only until Phase 14**, an accepted regression with the switcher kept working (finding 24).
- **Prices come from `data/products.json`; the price hold is empty** (the mechanism stays). Clinic-only is an explicit data flag, not "price 0". Cart messages are owner-approved and shown inline, never `alert()`.
- **A product panel derives availability from the data, never from markup** (`product-detail`): clinic-only comes from the `clinicOnly` flag, and stock is left to the cart's refusal, because stock changes and the live pages show no stock cue (findings 23, 31).
- **A FAQPage is derived from the faq-accordion's data, never carried** (`render.js faqPageNode`; `test:seo` fails a FAQPage that names different questions from the visible list). 常見問題 shows `data/faq.json`, the list the admin edits, not its old hand-coded Q&As.
- **Never emit rating or review data that doesn't exist** (P13G-T2 hard rule, enforced by `test:seo`, finding 26).
- **A pre-rendered page's stock changes go live at publish, not instantly.** Accepted; the client guide must say so at retirement.
- **Phase 9 definition-of-done waivers:** "heavy assets from R2" (a Phase 10 dependency). "Editable in Puck" now passes after the `mergeTree` fix.
- **One admin site, one login** is a client requirement (P11-T2).
- **Login throttle hardening** belongs to Phase 11 (P11-T3).

## Open questions to confirm with the project owner / client

- Where the API + database are hosted: Vercel + Atlas, or a small VPS. Pick one hosting story (currently split between Pages and Vercel). Phase 11.
- How much the client can change: delete a page, edit SEO, or admin-only?
- Does the shop need real online orders + payment, or stay staff-entered?
- **Client:**
  - the CJK font stack (finding 21b);
  - the health-claims review (P13G-T2);
  - whether the public GitHub Pages copy with invented ratings is taken down (finding 26, owner + client).

## Definition of done for a migrated page

- Renders from the tree, visually matches the current page within tolerance, at 1280 and 390, **scripts on and scripts off**, against the production-hostname baseline. Every difference is either explained or fixed.
- Pre-rendered: content is in the HTML, not fetched client-side.
- **Per-language assets checked explicitly, before the diff.** Any `src` or `href` the old page SWAPS by language (its `translations` / `productData` maps, or any runtime `.src =`) must be lifted into the tree as the **zh** value — never the URL hard-coded in the markup, which may be another language's. `npm run test:lang-assets` holds this for every retired page, with negative controls; run it, and read its per-page list, rather than hoping the visual diff shows it (finding 28: 小册子 would have published the German brochure to Chinese visitors, past SEO, fidelity and behaviour).
- SEO check passes: JSON-LD valid, hreflang reciprocal, canonical correct, meta present, heading order preserved.
- **Interactive behaviour ported and verified by effect** (BUILD_TASKS Appendix A, `docs/FINDINGS.md` finding 23). Everything the old page did for a visitor (cart, phone menu, quick view, language switch, refusals) either works, or is recorded as an accepted regression with its user impact stated. `npm run test:behaviour` asserts outcomes, such as the menu opening or the cart holding the right SKU at the right price, **with a negative control** proving the checks fail when the behaviour files are missing. "No errors" is not verification: 產品介紹 passed SEO, fidelity and the visual diff with every control dead and zero errors.
  - Behaviour lives in static files (`assets/site.js` shared, `assets/behaviour/<section>.js` per section), derived by `renderer/template.js` from the tree's section types. It never lives in section components (no hydration) or in inline scripts.
  - Bind by **SKU**, never by database id. Hooks are plain `data-sku` / `data-price` / `data-status`, never `data-wh-*`.
  - With scripts off, no control that needs JavaScript is shown.
- Editable in Puck; all target-language fields present or falling back to `zh`.
- Published output contains no editor-only attributes.
- Heavy assets referenced from R2/CDN, not the repo.
- **The live `<section>` wrappers are reproduced** (`wrap: { section, label, container }`). Those classes carry padding: dropping one moves everything below it. Check the original's `main > section` list against the rendered page.
- Old hand-coded HTML retired only after the above pass (moved to `legacy/`, not deleted).
